`GsonBuilder` is defined at `gson/src/main/java/com/google/gson/GsonBuilder.java:93`. It's a builder class holding all the configuration options for Gson (naming policies, exclusion strategies, type adapters, date formats, etc.).

Its `create()` method (`GsonBuilder.java:922`) is simple: it just returns `new Gson(this)` — constructing a new `Gson` instance configured from the builder's current settings. It's side-effect-free on the builder itself, so it can be called multiple times to produce multiple independently-configured `Gson` instances. The heavy lifting happens in `Gson`'s constructor, which assembles the final list of `TypeAdapterFactory` instances (via the builder's package-private `createFactories()` method shown above, combining built-in adapters, user-defined adapters, and exclusion strategies).
