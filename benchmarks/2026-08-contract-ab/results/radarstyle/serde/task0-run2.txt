The `Serialize` trait is defined at `serde_core/src/ser/mod.rs:234`. It's re-exported from the `serde` crate for public use (`serde::Serialize`), but the actual trait definition lives in `serde_core`.
